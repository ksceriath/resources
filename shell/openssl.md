Openssl is a toolkit implementing TLS cryptography.

It supports several commands.

s_client        : creates client which establishes connections to a remote server speaking TLS protocol.

e.g.

>> openssl s_client -connect host:port